spring:
  cloud:
    config:
      enabled: false
  config:
    import: "optional:configserver:"

eureka:
  client:
    enabled: false

app:
  security:
    users:
      - username: ivan
        password: ivan
        roles:
          - USER
      - username: anna
        password: anna
        roles:
          - USER
          - ADMIN
server.port: 8888

spring:
  main:
    banner-mode: off
    log-startup-info: false
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations:
            - file:../config-repo
            - file:../config-repo/telemetry
            - file:../config-repo/commerce
            - file:../config-repo/infra

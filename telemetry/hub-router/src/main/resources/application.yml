grpc:
  server:
    port: 59090

spring:
  application:
    name: hub-router
  config:
    import: configserver:http://localhost:8888

cloud:
  config:
    fail-fast: true
    retry:
      max-attempts: 6
      initial-interval: 1000
      multiplier: 1.1
      max-interval: 2000
